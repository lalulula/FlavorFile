import 'package:flutter/material.dart';

class EditRecipeBook extends StatelessWidget {
  const EditRecipeBook({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("EditRecieBookPage"));
  }
}
